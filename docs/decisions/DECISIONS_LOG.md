# Decisions Log

A single running log, not one file per decision — keeps overhead low for a
solo founder. Add an entry whenever a real decision (not a draft idea) gets
made. Distinguish status clearly: `approved` means acted on, `draft` means
still being considered.

| Date | Decision | Status | Rationale |
|---|---|---|---|
| 2026-07-12 | Scaffold engineering foundation (docs/, roadmap/, workflow/, .claude/skills/) alongside existing root docs | approved | Founder requested the engineering environment be built now, in parallel with (not instead of) commercial offer validation |
| 2026-07-12 | Reusable skills split by purpose: methodology as docs in docs/paof/, repeatable workflows as real Claude Code skills in .claude/skills/ | approved | Maximizes founder leverage — actionable skills get reused automatically instead of staying inert notes |
| 2026-07-12 | Dev workflow stays lightweight: direct commits to master for docs/config, branch+PR once product code is actively changing | approved | Solo founder, no collaborators yet — process should earn its keep before being added |
| 2026-07-12 | Default MVP delivery to Hostinger (hosting/cron/e-commerce/Reach) over provisioning n8n/Supabase/Twilio, escalating to a dedicated tool only for a specific gap the offer needs | draft | Hostinger MCP is already connected and covers hosting, cron automation, storefront, and email/CRM; candidate stack items aren't yet integrated anywhere — Buy/Integrate before Build |
| 2026-07-12 | Bake EU AI Act Art. 50 AI-disclosure into every chatbot/automation product template from the start; treat any HR-screening / credit-scoring / biometric use case as a stop-and-reassess high-risk gate | draft | Art. 50 transparency obligations apply from Aug 2, 2026 (very near-term); building disclosure in now avoids a costly retrofit — see `docs/paof/ai-governance-security.md` |
| 2026-07-12 | First validation customer = founder's schoonmoeder's uitvaartonderneming (warm relationship, real recurring admin pain). Start with ONE low-emotional-risk back-office task, NOT the generic platform and NOT the AI phone assistant | draft | Solve one concrete case config-driven; the generic "any niche" core emerges from 2-3 real cases, not before (avoids premature generalization). Phone AI = highest emotional risk + Art. 50 exposure = deferred |
| 2026-07-12 | Do a structured time-audit interview with schoonmoeder before building anything, to pick the first automation wedge objectively (time saved × low risk × feasibility) | approved | Founder chose this over guessing the pain; matches offer-validation method (problem before solution). Guide: `clients/ab-uitvaartzorg/docs/discovery-schoonmoeder-uitvaart.md` |
| 2026-07-12 | Candidate first wedge: a knowledge-grounded AI chatbot on the AB Uitvaartzorg website for general/practical visitor questions, with a prominent "bel Alien" escape for urgent/sensitive cases | draft | Strong Fase-A candidate: lower risk than phone AI, dual value (time + 24/7 customer value), Integrate/Configure not Build, and the grounded knowledge base IS the reusable "generic core, configurable vertical" pattern. Non-negotiables: brand-matching tone, grounded in Alien's real content (anti-hallucination), Art. 50 AI-disclosure, minimal GDPR-safe logging. Premise ("reduces general-question phone load") is tested by discovery Q5/Q7, not assumed |
| 2026-07-12 | Fast-track the website chatbot as the first PrimeCircle build (build-first, validate with real usage) instead of waiting for the time-audit to decide | approved | Founder's call: low-risk, reversible, dual-value, and Alien is a family design partner. Approach = own knowledge-grounded widget (Integrate/Configure), NOT external SaaS bot; stateless v1 (no PII stored); staging + Alien sign-off before live. Spec: `clients/ab-uitvaartzorg/docs/chatbot-ab-uitvaartzorg-spec.md`. Model/provider deliberately not yet chosen (verify current options/pricing before commit) |
| 2026-07-12 | Chatbot architecture: context-stuffing (whole knowledge base in the prompt), NOT a vector DB / RAG | approved | KB is ~2-3k tokens — fits in context with huge headroom; simpler, more accurate (no retrieval can miss a chunk), nothing to maintain. Revisit RAG only if a single client's KB grows to hundreds of pages |
| 2026-07-12 | Chatbot model = Anthropic Claude Sonnet 5 (`claude-sonnet-5`), direct Anthropic API, thinking off / low effort | approved | Founder chose Sonnet 5 (verified current pricing): strong Dutch + warm tone + strict instruction-following for the safety boundaries; cost negligible at this volume (~$7.50/1,000 chats, intro pricing through 2026-08-31). Haiku 4.5 = scale fallback, Opus 4.8 = overkill. Needs an Anthropic API key |
| 2026-07-14 | Widget integrated into the existing AB Uitvaartzorg site (14 NL root pages + 13 EN pages in `en/`) via one `<script>` tag per page; widget made bilingual (data-lang nl/en) and endpoint takes a `lang` hint for reliable English | approved | Founder asked to connect the chatbot to the existing local site (NL+EN). Bilingual + lang-steering kept. NOTE: the site folder has a `.git` dir but is NOT a valid git repo — reversibility is via `chatbot/site-integration/apply.mjs remove` (round-trip tested), not git |
| 2026-07-14 | Switched from endpoint-served widget to bundling the widget assets INTO the site (`assets/ab-chat.js` + `ab-chat.css`, copied by `apply.mjs`, relative-path embed) | approved | Endpoint-served meant the button only appeared when the server was running — founder opened the site and saw nothing. Bundled assets make the button appear on any page open (even file://, no server); only the chat call needs the endpoint (graceful fallback). Canonical source stays `chatbot/public/`; `apply.mjs add` re-syncs the copy and bakes in the endpoint host |
| 2026-07-14 | Vendored the third-party `scroll-world` skill (MIT) into `.claude/skills/` for possible future use | approved | Founder wants the capability on hand (scroll-scrubbed "fly through the world" landing pages via Higgsfield + ffmpeg). Safety-scanned before install (benign image/DOM helpers). Not authored by us; requires external tooling (Higgsfield account/CLI, ffmpeg) to actually run — installed as a dormant capability, not an active dependency |
| 2026-07-14 | Switched the chatbot from Anthropic direct (Sonnet 5) to **OpenRouter**, model `google/gemini-2.5-flash-lite` (verified current pricing, ~€0.30/1,000 chats), via zero-dependency `fetch` (dropped `@anthropic-ai/sdk`) | approved | Founder supplied an OpenRouter key and asked for the cheapest suitable model. Chosen for strong Dutch+English, good instruction-following, huge context, trivial cost; model is env-configurable (`OPENROUTER_MODEL`). Key stored in gitignored `chatbot/.env`. **First real end-to-end validation done**: grounded costs, safety escalation on grief input, no invented prices, EN steering — all pass. Tightened system prompt to refer to Alien in the third person (no impersonation) |
| 2026-07-14 | Package the whole uitvaart stack (website NL+EN + chat) as ONE isolated Docker project for the founder's Hostinger VPS, single container serving site + `/api/chat` on one origin | approved | Founder wants it always-on (not `node` on their laptop), in its own container/network so nothing mixes with other VPS projects. Built `clients/ab-uitvaartzorg/deploy/` (Dockerfile + compose + `assemble.mjs` that rebuilds app/+site/ from canonical sources and sets the widget to same-origin). Port `APP_PORT=8091` (founder opens it in the VPS firewall). Verified locally container-style: serves NL/EN pages, assets, and live same-origin chat. Founder does the actual deploy + firewall; no VPS access from here |
| 2026-07-14 | Refactor the chatbot into a **config-driven, reusable template** (chosen by founder for the 2-week Alien-unavailable window) | approved | Turns the one-off AB build into a sellable product — the "generic core, configurable vertical" thesis. Per-customer folder `chatbot/customers/<slug>/{config.json, system-prompt.txt, knowledge-base.md}`; one generic `server.js` (serves `/api/config`) + one generic `widget.js` (fetches branding/phone/colors/langs at runtime). New customer = copy `_template`, fill config + KB, set `CUSTOMER`. **Proven**: same code re-branded live for a 2nd demo customer (Bakkerij De Korenbloem) via one env var — different colors, phone, and grounded knowledge. Deploy carries the chosen `CUSTOMER` |
| 2026-07-14 | Add production hardening + privacy-safe analytics + suggested-question chips (founder: build all shortlist items) | approved | **Hardening**: per-IP rate limiting on `/api/chat` (env `RATE_LIMIT_PER_MIN`, default 30 → 429 with friendly reply), `/health` endpoint, stdout access logging. **Analytics**: append-only JSONL per chat with metadata only by default (lang, lengths, latency, referred-to-phone) — raw question text ONLY if `LOG_QUESTIONS=true` (privacy opt-in; needs privacy-statement + retention), plus `stats.mjs` summary (usage, referral rate, FAQ backlog). Docker volume persists it. **UX**: per-customer suggested-question chips from `config.json`, guiding visitors to answerable topics (also lowers hallucination risk). All verified live (429 after limit, /health, analytics written, chips in /api/config) |
| 2026-07-14 | Cache-busting on the widget embed + a token-protected analytics dashboard (founder: chips weren't showing = browser cache; wanted visual monitoring) | approved | **Cache-busting**: `apply.mjs` now appends `?v=<content-hash>` to the embed `src`, so browsers auto-load the new widget when it changes (root cause of "chips niet zichtbaar" was the cached old `ab-chat.js`). **Dashboard**: `/dashboard?token=…` (token-gated HTML, vanilla/no-deps: KPIs, per-day bar chart, per-language, referral rate, FAQ backlog) + token-gated `/api/stats` JSON; `DASHBOARD_TOKEN` env (empty = disabled). Verified: 403 without/with wrong token, 200 with correct token, data renders |
| 2026-07-14 | Deployed the stack to the founder's Hostinger VPS via SSH (Docker) — container runs as non-root in `/opt/ab-uitvaartzorg` | approved | Founder asked to move website + chat + dashboard to the VPS. Deployed with `deploy-to-vps.sh` (tar over SSH, key `primecircle_codex_vps`, `root@31.97.123.34`), isolated container/network/volume (`ab-uitvaartzorg`), `restart: unless-stopped`, `USER node`. Verified end-to-end incl. live OpenRouter chat. Client code still to be pushed to a private `CryptoWesAI/ABUitvaart` repo (not the primecircle workspace) |
| 2026-07-14 | Put the deployment behind the VPS's existing Traefik with a free staging domain → **live on `https://ab.primecircle.cloud`** (HTTPS), and removed the raw public port | approved | Alien's real domain isn't ready/approved, so bought nothing — claimed Hostinger's free-with-VPS domain `primecircle.cloud` (renews ~₹2536/yr, disposable staging domain, subdomains per project). DNS `ab.primecircle.cloud` → VPS. Copied the proven Traefik label pattern from the existing `knifensharp` service (host-mode Traefik, `certresolver=letsencrypt`, service port 8091). Let's Encrypt cert issued + HTTP→HTTPS redirect verified. Dropped the `ports:` mapping so access is HTTPS-only (raw 8091 now closed). Rotated `DASHBOARD_TOKEN` (old one had been shown in chat). Auto-renew disable was blocked by the safety classifier (financial action) — founder toggles it in hPanel |
| 2026-07-18 | Belvanger contact = WhatsApp + e-mail only; founder's phone number fully removed as a "call me" affordance (all `tel:` links + visible digits gone from site, legal pages, JSON-LD, and chat widget). Shared chat widget made channel-aware (phone → WhatsApp → email, backward-compatible so AB keeps its phone escape) | approved | Founder wants his personal number off the page. WhatsApp buttons relabelled to text ("Stuur een WhatsApp") so no digits show; `info@belvanger.nl` added everywhere as written alternative. Widget config gains `whatsapp`/`contactEmail`; Belvanger `config.json` phone emptied. All verified live (0 `tel:` links, no visible number, bot no longer gives a phone number). Real dedicated WhatsApp Business number deferred to the missed-call build |
| 2026-07-18 | Missed-call-textback MVP stack locked after founder interview: take over the trade's existing number via conditional call-forwarding (no-answer) → programmable number; customer channel = WhatsApp (branded Business profile, separate API number, one approved Utility template); orchestration = **n8n self-hosted on the VPS** (not n8n Cloud) | approved | Founder answers folded into `docs/build/mvp-missed-call-textback.md`. Self-host n8n = €0 vs €20–50/mo Cloud, data on own EU-VPS, beheer is a learning goal → Stance Configure. Honest constraints surfaced: WhatsApp can't send from the trade's exact digits (branded profile carries recognition instead); first message must be an approved Utility template; full Meta business verification is an upscale checkpoint, not a pilot blocker (no KvK yet) |
| 2026-07-18 | Built a **private VPS secrets + restart dashboard** (`infra/dashboard/`, deployed `/opt/dashboard`): per-project `.env` view/add/edit/delete + "save & restart" (`docker compose up -d`), PrimeCircle dark style. Founder chose **private-only access** (Tailscale) + **custom build** over Infisical, and accepted the Docker-socket approach for v1 | approved | Real pain: 9+ containers, secrets scattered across `.env` files. Security-first: served on `127.0.0.1` via `tailscale serve` (NOT the public Traefik — never on the open internet), app-login (scrypt), CSRF header, backup-before-write, `chmod 600`. Managed set = the `.env`-based projects (ab, belvanger, glasservice, n8n); compose-`environment:` projects are read-only in v1. Pipeline verified end-to-end on a throwaway project (list→reveal→save→restart→container picks up new env; backup holds the pre-change value). Remaining: founder enables HTTPS certs in Tailscale admin, then `tailscale serve`. v2 hardening = privilege-separation to drop the socket. Spec: `infra/dashboard/SPEC.md`, ops: `DEPLOY.md` |
| 2026-07-18 | Stood up **n8n on the VPS** at `https://n8n.primecircle.cloud` (Docker + existing Traefik, pinned `n8nio/n8n:2.30.7`, own network + persistent volume, encryption key in gitignored `/opt/n8n/.env`) | approved | Step A of the missed-call build — the orchestration "brain", zero-risk/€0. Added DNS A-record `n8n` → 31.97.123.34 (no wildcard existed). Source of truth in repo `infra/n8n/`. Verified: healthz ok, valid Let's Encrypt cert, awaiting owner-account setup (n8n's first-run). Founder must claim the owner account on first visit. Next: Twilio number (B) + WhatsApp API/template (C), then build the n8n flow (D) |
| 2026-07-14 | Next track = supplier-ordering + invoicing app (Fase B); recommend Configure/Automate (self-hosted NocoDB/Baserow + n8n on the VPS) over Build (custom Next.js/Supabase) | draft | Founder shared the concept (catalog/"stamboom" of products/services → suppliers; enter the agreed pakket → auto order-emails per supplier → later auto-invoice). Low volume + solo operator → building a custom platform first is the founder-time trap. Low-code + n8n prototypes the whole flow in days, self-hosted (GDPR/EU, vendor-agnostic). Build custom only if the low-code UI proves too limiting. Not yet designed/approved |
| 2026-07-25 | Belvanger klantdashboard naar Android = **PWA-first, dan Trusted Web Activity** (Bubblewrap-equivalent Gradle-project + GitHub Actions), NIET Capacitor / React Native / Flutter / WebView-wrapper / no-code | approved | Founder vroeg om het dashboard "in zijn volledigheid" als Play Store-app. Deze route port geen frontend (het bestaande `public/` IS de app), voegt geen bundler of framework toe aan een bewust dependency-arme codebase, en app-versie 1.0 blijft 1.0 omdat elke UI-wijziging serverside meegaat zonder store-review. Geverifieerd feit: TWA-op-een-echte-PWA is Google's eigen route langs beleid 4.3 ("minimum functionality"), waar een WebView-wrapper juist op wordt afgekeurd, en de TWA draait de volledige Chrome-engine dus service worker + Web Push werken erin. Bouwt in CI met alleen een JDK: geen Android Studio, geen Mac. Gebouwd + lokaal geverifieerd; publicatie wacht op founder (D-U-N-S, DNS, keystore). Route en gotchas: `sites/belvanger-portal/ANDROID.md`, onderbouwing: `docs/research/belvanger-android-app-adhd-onderzoek-2026-07-25.md` |
| 2026-07-25 | Web Push zelf geïmplementeerd op `node:crypto` (`src/webpush.js`) i.p.v. de `web-push`-package | approved | Codebase is bewust dependency-arm (alleen `pg`) en heeft om dezelfde reden al een eigen SMTP-client. Web Push is drie afgebakende RFC's (8188/8291/8292) en geen bewegend doel, dus een eigen implementatie is minder onderhoud dan een dependency-boom om te patchen. Risico van geen tweede implementatie die je corrigeert is afgedekt met `tests/webpush-rfc8291.mjs`, die tegen het officiële testvector uit RFC 8291 Appendix A valideert (body, header-layout, randgevallen, VAPID-sleutelvorm) — slaagt |
| 2026-07-25 | Play Console onder **organisatie**-identiteit (KvK + D-U-N-S), niet persoonlijk; en géén productie-upload voordat een klant een expliciete app-regel betaalt | approved | Geverifieerd feit: persoonlijke Play-accounts van na 13-11-2023 moeten eerst 12 testers × 14 aaneengesloten dagen closed testing doorlopen; organisatie-accounts op een geregistreerde rechtspersoon zijn daar volledig van vrijgesteld. Scheelt "twaalf mensen overhalen" en houdt het privéadres uit de listing, maar kost 1-4 weken wandklok voor D-U-N-S, dus die aanvraag moet als eerste lopen. De prijsregel is de tegenmaatregel tegen de gedocumenteerde founder-valkuil van onbetaalde overhead: store-onderhoud (targetSdk-tredmolen, jaarlijkse beleidswijzigingen, Data Safety opnieuw verklaren) is permanent en niet achteraf te factureren |
| 2026-07-25 | Sms-terugval bij niet-afgeleverde pushmelding NIET meegebouwd (ontworpen, uitgesteld) | draft | Push heeft geen afleverbewijs in het protocol en OEM-batterijbeheer sloopt bezorging stil, dus een escalatieladder (push → geen ACK binnen 30s → sms) is de juiste volgende stap en scoorde het hoogst in het onderzoek. Maar elke terugval kost Twilio-geld per bericht: dat is een beslissing over klantkosten en dus founder-terrein. Wat er nu wel is: `push_devices.failure_count`/`last_success_at` als storingssignaal, plus de bestaande e-mailmelding als vangnet. Ontwerp (Postgres als timer, `lead_alerts` + `lead_alert_attempts`, ACK via service worker met per-alert HMAC) staat in het onderzoeksdocument |
| 2026-07-25 | **Play Store-publicatie geparkeerd.** Leveringsmechanisme = de geïnstalleerde PWA op het beginscherm, niet een Android-app in de store. Het TWA-project en de CI-workflow blijven staan als slapende optie | approved | Founder-besluit direct na het onderzoek, en het is dezelfde conclusie als de aanbeveling: de PWA geeft de vakman 100% van de functionele waarde (icoon, volledig scherm, offline, meldingen) en dekt gratis ook iPhone-gebruikers. De store voegt alleen vindbaarheid en legitimiteitsgevoel toe, tegen permanente onbetaalde onderhoudslast (targetSdk-tredmolen, jaarlijkse beleidswijzigingen, Data Safety opnieuw verklaren). Kosten van parkeren zijn nul: `android/` en de workflow zijn `workflow_dispatch`-only, dus er gaat nooit per ongeluk iets naar Google, en de route staat gedocumenteerd in `ANDROID.md` als een klant er ooit om vraagt. **Belangrijke nuance die geen bookmark oplost:** iOS Safari geeft Web Push alleen aan PWA's die op het beginscherm staan, dus de klant moet door "App installeren" en niet door "bookmark maken" worden geleid. Op Android werkt push ook in een gewone tab, daar is installeren winst op weergave en vindbaarheid. Gevolg: een expliciete installeer-affordance in het dashboard is nu load-bearing en nog niet gebouwd |
| 2026-07-25 | PWA-laag + meldingen **live gedeployed** naar `https://dashboard.belvanger.nl` (VPS `/opt/belvanger-portal`), met VAPID-sleutels in de gitignored `.env` op de VPS | approved | Founder gaf expliciet go. `.env` eerst geback-upt (`.env.bak-20260725-105448`), VAPID-sleutels op de VPS zelf gegenereerd in een wegwerpcontainer zodat de private key nooit door een transcript liep, `chmod 600`. `PUSH_INCLUDE_CALLER=false` gezet: het nummer van de beller in de pushmelding blijft een expliciete founder-keuze (payload is wel end-to-end versleuteld, dus aanzetten is verdedigbaar). Live geverifieerd: `manifest.webmanifest` als `application/manifest+json`, `sw.js` met `no-cache` + `Service-Worker-Allowed`, iconen als `image/png`, `push_devices`-tabel aangemaakt bij boot, en in een echte browser een **geactiveerde service worker** op scope `/` met een manifest dat parseert (3 iconen, display standalone). `/.well-known/assetlinks.json` geeft 404, zoals bedoeld nu de store geparkeerd is |
| 2026-07-25 | Bug gevonden en gefixt tijdens de deploy: `serveStatic` behandelde alleen GET, dus **elk HEAD-verzoek gaf 404**, ook HEAD op `/` | approved | Kwam boven doordat een verificatie met `curl -I` ineens `no-store` en een ontbrekende `Service-Worker-Allowed` liet zien; de app was goed, de meting gebruikte HEAD. Latent maar reëel: vrijwel elke uptime-monitor gebruikt standaard HEAD en zou de hele site als down rapporteren. Router accepteert nu GET en HEAD, `serveStatic` stuurt bij HEAD dezelfde headers zonder body, en er gaat nu ook een `Content-Length` mee. Live geverifieerd: HEAD geeft 200 met 0 bytes body, GET onveranderd |
| 2026-07-25 | `PUSH_INCLUDE_CALLER=true` op productie: de pushmelding bevat het nummer van de beller plus een "Bel terug"-knop die direct de telefoonapp opent | approved | Founder-besluit. Verdedigbaar omdat de payload end-to-end versleuteld is (RFC 8291) met sleutels die alleen het toestel van de klant heeft, dus de push-dienst ziet uitsluitend ciphertext, en het betreft de eigen leaddata van de klant naar zijn eigen toestel. Zonder nummer moet iemand op een ladder eerst de app openen en inloggen, wat de belofte "mis nooit een klant" ondermijnt. `.env` opnieuw geback-upt vóór de wijziging; container ziet de vlag (`printenv` = true), health = healthy, live endpoints onveranderd. Terugdraaien is één regel plus een herstart |
| 2026-07-25 | Promotiefilm "Opgevangen" volledig in huis gemonteerd (31s, 1080×1920) i.p.v. losse clips die de founder zelf in CapCut moest samenvoegen. ffmpeg als statische build in een tijdelijke map, niet op de machine geïnstalleerd | approved | Founder vroeg "kun je de video niet gewoon maken in OpenArt, met geluid". Antwoord was deels ja: Seedance 2.0 kan wél 15s mét gesynchroniseerd geluid (act 1 is nu één doorlopende clip met regen + ringtone, 1200 credits), maar kan géén leesbare tekst renderen en zit op 15s maximum, dus een film van 31s uit één generatie is onmogelijk. Oplossing: de bestaande hero-simulatie als frame-reeks opnemen op exact 1080×1920 (23,1 fps gehaald met JPEG-capture; PNG haalde maar 16 fps), tekstkaarten uit HTML met de echte merkletters, en alles met ffmpeg monteren. Reproduceerbaar via `sites/belvanger/film/monteer.sh` + `neem-simulatie-op.mjs`. ffmpeg bewust als statische build in de scratchpad i.p.v. `winget install`: geen systeemwijziging op de machine van de founder |
| 2026-07-25 | Twee fouten gevonden door de eindfilm frame-voor-frame te bekijken en de audio te meten | approved | (1) **Dubbele beat**: act 1 eindigde met een rinkelende telefoon en act 2 begon met opnieuw 3,4s rinkelend gespreksscherm uit de simulatie. Act 2 start nu op 3,0s zodat de film direct naar "Gemiste oproep · 14:32" knipt. Scheelde 3s en haalde een herhaling weg. (2) **Geluid veel te zacht**: gemiddeld -33 dB, op een telefoon nauwelijks hoorbaar. Nu genormaliseerd naar EBU R128 (-16 LUFS, TP -1,5 dBTP), gemeten resultaat gemiddeld -23,8 dB met peak -1,6 dB. Beide zouden zonder verificatie ongemerkt zijn meegegaan |
| 2026-07-25 | Pushmeldingen uitgebreid naar **inkomende reacties**: `sms.inbound`, `email.inbound` en `chat.lead` geven nu ook een melding, niet alleen `call.missed` en `website.lead` | approved | Founder-besluit. De gemiste oproep is het probleem, maar het ANTWOORD van de beller is de lead; bleef dat ongemeld, dan miste de klant het gesprek alsnog en ondermijnt dat de belofte "mis nooit een klant". `sms.inbound` was gevraagd; `email.inbound` en `chat.lead` zaten met exact hetzelfde defect (een lead via de chatbot bleef stil terwijl `website.lead` wel meldde), dus alle drie meegenomen. Bewust NIET meldend: `sms.outbound`/`sms.status` (ons eigen systeem aan het werk) en de dashboardacties `contact.status`/`contact.manual`/`contact.referred` (die doet de klant zelf, een melding van jezelf is ruis). Bevestigd tegen `n8n/README.md` dat alle drie de events daadwerkelijk door n8n verzonden worden, dus de melding vuurt echt |
| 2026-07-25 | Meldingsteksten en gegevensminimalisatie naar een eigen, testbare module `src/push-payloads.js` met `tests/inbound-push.mjs` | approved | `server.js` start bij import een HTTP-server en eist `DATABASE_URL`, dus daar valt niets uit te testen. Deze logica bepaalt letterlijk wat een vakman op zijn telefoon ziet en of daar klantgegevens in staan; een fout is stil (de melding komt aan, alleen met te veel of te weinig erin). Test dekt: welke events wel/niet melden, gedrag met `PUSH_INCLUDE_CALLER` aan en uit, terugval op nummer als de naam onbekend is, afkapping op 140 tekens, opschonen van regeleindes uit een sms, en de payloadvorm die `sw.js` verwacht. Verder: `tag` per CONTACT i.p.v. per event, zodat drie berichten achter elkaar de melding vervangen in plaats van een stapel bouwen, en de "Bel terug"-knop alleen bij `sms.inbound` (bij e-mail en chat is er meestal geen nummer, en een dode knop is erger dan geen knop) |
| 2026-07-25 | Inkomende-reactie-meldingen **live gedeployed** naar `https://dashboard.belvanger.nl` | approved | Founder gaf go. Geen nieuwe env-variabele nodig, dus `.env` onaangeroerd (23 sleutels, ongewijzigd). Geverifieerd na deploy: container `running/healthy`, `src/push-payloads.js` zit in het image, de ingest-hook staat op regel 644, en `tests/inbound-push.mjs` is tegen de GEDEPLOYDE broncode gedraaid in een wegwerpcontainer en slaagt. Live endpoints onveranderd (`/` 200, `/healthz` ok, manifest met het juiste content-type). Laatste onbekende blijft de praktijktest: iemand moet een sms terugsturen naar het Twilio-nummer om te zien of de melding met de tekst erin aankomt |
| 2026-07-27 | Diagnose testomgeving: de automatische sms werd wél verstuurd maar geblokkeerd (Twilio 30007, carrier violation). Eén oorzaak, drie gevolgen. Founder houdt de testopstelling bewust zo en koppelt een echt NL-nummer bij de eerste klant | approved | Uitsluitend read-only vastgesteld via de Twilio- en n8n-API's; niets gewijzigd. Oorzaak: de Studio Flow (het `voice_url` van `+14474274008`) verzendt vanaf de alfanumerieke afzender `Belvanger` i.p.v. vanaf het nummer. Gevolgen: (1) NL-providers filteren niet-geregistreerde alfanumerieke afzenders bij herhaling, dezelfde tekst naar hetzelfde nummer ging op 19 en 20 juli wél door en op 26 juli niet; (2) een alfanumerieke afzender is éénrichtings, dus de beller kán niet antwoorden, waardoor `sms.inbound` (en de vannacht gebouwde melding daarop) nooit kan vuren en act 2 van de promotiefilm in de praktijk onmogelijk is; (3) het importfilter in de code-node "Maak sms-berichten klaar" matcht op `message.from === testNumber` en slaat de sms daardoor **stil** over, dus Twilio leverde 3 sms'jes af terwijl het dashboard er 1 kent. Founder-besluit: testopstelling blijft, echte nummer komt met klant #1. Bewust NIET gewijzigd door mij: Studio Flow, `sms_url` (staat nog op `demo.twilio.com`, zou een demo-antwoord naar een klant sturen) en de n8n-code |
| 2026-07-27 | README-claim over de bewijslog gecorrigeerd: "elke `call.missed` is per definitie al opgevangen" was feitelijk onjuist | approved | Die zin onderbouwt de bewijslog richting de klant en zou zo in een aanbieding kunnen belanden. Workflow 1 is in werkelijkheid een poller die `call.missed` schrijft ongeacht of er een sms uitging; de sms komt uit een Twilio Studio Flow, niet uit n8n. README benoemt nu de feitelijke werking, het gemeten gevolg, en de expliciete voorwaarde dat "opgevangen" niet als bewijs aan een klant mag worden gepresenteerd zolang de afzender en het importfilter niet zijn opgelost |
| 2026-07-27 | "Bel terug" vanuit een pushmelding werkt: tik op de melding → telefoonapp (met eenmalige Android-bevestiging). Zes testrondes gekost; oorzaak alleen gevonden door de service worker zelf te laten rapporteren | approved | Twee echte fouten, plus een derde die ik zelf introduceerde. (1) `clients.openWindow("tel:...")` neemt geen niet-http-schema aan: Chrome opende een browsertab MET `tel:+31...` in de adresbalk. Dat stond al in mijn eigen commentaar en is toch als eerste poging toegevoegd. (2) **Het gebruikersgebaar van een notificationclick vervalt bij de eerste `await`**, waarna Chrome weigert met `InvalidAccessError: Not allowed to open a window`; met `vensters=0` (app dicht) gebeurde er dus helemaal niets. (3) De `await diag(...)` die ik toevoegde om dit te méten was zelf de await die het gebrek veroorzaakte. Fix: `clients.openWindow(target)` is nu de eerste regel van de handler zonder await ervoor, geen `tel:` meer in openWindow, en de terugvalroute (`matchAll`/`navigate`/`focus`) komt erna. Ook gefixt: de belbalk verdween op `visibilitychange`, wat ook vuurt bij het dichtklappen van het meldingenpaneel. Diagnostiek is na de vondst weer verwijderd (route geeft weer 401) en de 7 testevents zijn opgeruimd |
| 2026-07-27 | Service-workerversie zichtbaar in het dashboard, met zelfservice-reparatieknop | approved | Een service worker die vast blijft zitten op oude code is de meest verraderlijke storing in een PWA: alles lijkt te werken, de push-dienst meldt "aangenomen", en het toestel gedraagt zich als deploys terug. Serverside is dat niet te zien, en bij een klant kan de founder diens telefoon net zo min uitlezen. De meldingenkaart toont nu de actieve `SW_VERSION` tegen een verwachte waarde; wijkt die af of reageert de worker niet, dan verschijnt "Meldingen opnieuw instellen", die registratie, push-subscription en caches hard weggooit en herlaadt. Dit was ook het middel waarmee de bovenstaande oorzaak kon worden uitgesloten in één stap in plaats van nog een blinde deploy |
| 2026-07-27 | Vier omgevingsingrepen na ADHD-onderzoek: (1) al het werk in git, (2) deny-list op destructieve verbs, (3) rollback-script mét echte restore-test, (4) `SELLING.md` zonder hook | approved | Uitkomst van 38 ideeen uit zes geisoleerde branches. Geverifieerde aanleiding: laatste commit was 21 juli met 77 onopgeslagen bestanden, `~/.claude/settings.json` bevatte alleen `{"model":"opus"}`, `.gitignore` noemde `.claude` niet, en `deploy-to-vps.sh` maakte wel backups maar er was geen weg terug (71 stuks, 136 MB, nooit teruggezet). **Bewust NIET gedaan**, want traps: geen eigen Remotion-skill (upstream `remotion-dev/skills` bestaat), geen npm-kit, geen brand-as-code, geen jobs-wachtrij, geen vijfde kennisbron (`docs/GOTCHAS.md`): `LEARNINGS.md` heeft al een `Encoded in:`-regel die naar een uitvoerbaar bestand kan wijzen in plaats van naar een gewoonte |
| 2026-07-27 | Deny-list globaal i.p.v. per project, en pruning in de rollback opt-in gemaakt | approved | Globaal omdat de Hostinger-MCP-servers user-scoped zijn: een project-deny geldt dan niet vanuit een andere map. 18 deny-regels (exacte V1-namen plus wildcards zoals `DNS_reset*`, zodat een toekomstige V2 niet ongemerkt door de mazen glipt) en 3 op ask (`ssh`, `scp`, `DNS_updateDNSRecordsV1`). Geen allowlist en geen hooks: die komen pas na een week meten hoe vaak de ask-laag echt vuurt, anders is de wrijving op gevoel gedimensioneerd. Direct aantoonbaar effect: de destructieve Hostinger-tools verdwenen tijdens de sessie uit het beschikbare gereedschap. **Belangrijke beperking, expliciet vastleggen:** de deny-list bindt NAMEN, niet capaciteit. `ssh root@31.97.123.34` is root op de machine die belvanger.nl, het portal en n8n draait en reproduceert elke geweigerde verb zonder ceremonie. De echte winst is dus niet de poort maar de terugweg. Pruning in `rollback-to-vps.sh` is daarom opt-in (`--prune`): een rollback die stilzwijgend 66 backups van een klantproductie wist is een verrassing, en zeker niet tijdens een test |
| 2026-07-27 | Rollback-script `sites/belvanger/rollback-to-vps.sh` echt restore-getest op productie | approved | Risicoloze maar echte test: eerst een momentopname van de HUIDIGE staat gemaakt, die teruggezet, en de md5 van `site/index.html` voor en na vergeleken (identiek, dus netto nul verandering). Volledige pad doorlopen: dry-run, container recreate, healthcheck, controle van de live URL. **Bevinding uit de test:** poging 1 gaf HTTP 404 en poging 2 gaf 200, dus er is een korte uitvalperiode tijdens het herstarten van de container. Dat geldt dus ook voor elke gewone deploy. De wachtlus in het script is daarmee geen decoratie. Vorige staat blijft bewaard als `pre-rollback-<tijdstempel>`, dus een rollback is zelf terug te draaien |
| 2026-07-27 | `SELLING.md` aangemaakt, maar de SessionStart-hook BEWUST niet | approved | De drie cijfers (prospects gesproken, gesprekken geboekt, gefactureerd) worden afgeleid uit een grootboek met echte namen en niet bovenaan ingetypt: een cijfer verhogen is dan een muisklik, een regel toevoegen vereist het verzinnen van een mens. De hook is expliciet gegate op één echte grootboekregel, omdat het bouwen van deze machinerie precies de vorm is die het uitstelgedrag hier aanneemt: een avond aan een verkoopteller voelt als verkoopwerk en is het niet. Ships de machinerie eerder dan het eerste gesprek, dan is de machinerie zelf het uitstel. Ook vastgelegd: de expiry-conditie (bij drie betalende klanten roteren de cijfers naar MRR, support en opzeggingsrisico), want een teller zonder einddatum wordt achtergrondschuld |
| 2026-07-28 | `dcg` (Destructive Command Guard) geinstalleerd als PreToolUse-hook, met vier extra pakketten aan en een valse regel op `warn` | approved | Founder-verzoek. Dit is de sterke versie van de deny-list van 27 juli, waarvan toen al is vastgelegd dat die NAMEN bindt en geen capaciteit. dcg leest de commandotekst zelf, ook binnenin heredocs en `docker exec ... psql -c "..."`, wat gemeten is: alle drie de SQL-varianten worden geblokkeerd. Geinstalleerd vanuit een lokale clone en niet via een pipe van internet; checksum verplicht geverifieerd, minisign-handtekening ontbrak bij deze release (v0.7.3) en dat is een bekende beperking. Geen beheerdersrechten, PATH op gebruikersniveau, bestaande hooks in `~/.claude/settings.json` bleven staan ("0 unrelated hooks preserved"). **De standaardinstelling was niet genoeg**: gemeten dat `docker volume rm belvanger-portal-db`, `docker compose down -v` en `DROP DATABASE portal` er ongehinderd doorheen gingen, precies de drie commando's die de leadhistorie van de klant in een klap wissen. Daarom `containers.docker`, `containers.compose`, `database.postgresql` en `database.sqlite` aangezet; daarna alle negen destructieve testcommando's DENY en alle zes werkcommando's ALLOW. **Wrijving eerlijk vastgelegd:** de regel `windows.filesystem:windows-filesystem-semantic-unverified` slaat aan zodra het uitvoerbare bestand uit een variabele komt (`& $dcg test "git status"`), een alledaagse PowerShell-schrijfwijze; hij blokkeerde drie onschuldige commando's op rij, waaronder het commando waarmee ik dcg zelf wilde testen. Die staat nu op `warn` en niet uit, omdat de echte bescherming in een andere regel zit (`remove-item-recurse-force`, nagemeten dat die blijft blokkeren). Vijf handelingen van deze week zouden nu geblokkeerd zijn: `docker rm -f <testcontainer>`, `git branch -D`, `rm -rf <scratchpad>`, `Remove-Item -Recurse -Force` en het opruimen van een wegwerpdatabase. Dat is de bedoelde prijs, niet een defect; `dcg allow-once` is de uitweg. Raakt de geplande back-uptaak NIET: die draait via Windows Taakplanner en niet via een agent, dus de hook zit er niet tussen |
| 2026-07-29 | KvK-inschrijving komt op de handelsnaam **Belvanger**, en PrimeCircle wordt bewust NIET ingeschreven | approved | Founder vroeg onder welke naam hij moest inschrijven; hij wilde PrimeCircle als paraplu zodat een volgend merk geen tweede KvK nodig heeft. Die zorg bleek ongegrond: een eenmanszaak mag onbeperkt handelsnamen voeren onder een KvK-nummer, en een naam toevoegen is gratis via Mijn KVK (geverifieerd op kvk.nl, niet uit het hoofd). De paraplu is dus de rechtsvorm, niet een naam, en daarmee werd het een merkvraag in plaats van een juridische. Belvanger is wat de klant betaalt en wat op factuur, domein, dashboard en film staat, dus daar komen de papieren op. PrimeCircle blijft eruit omdat het een naam is die de founder met anderen deelt: handelsnaamrecht ontstaat door GEBRUIK, niet door inschrijving, dus alleen inschrijven en gebruiken bouwt in je eentje een claim op een gedeelde naam. Twee klikken werk, jaren later moeilijk terug te draaien, en het bemoeilijkt precies de samenwerking die je later zou willen (dat wordt dan sowieso een VOF of BV met een eigen inschrijving). Intern blijft PrimeCircle gewoon de werknaam van de repo en het denkraamwerk; de KvK hoeft niet te weten hoe de mappen heten. Gevolg dat hieruit volgt: een klantsite hoort niet op `ab.primecircle.cloud` te blijven staan zodra er een betalende klant is. Timing: niet inschrijven voor de vakantie (31 juli tot 11 augustus), want er is nog niets te factureren en een inschrijving start meteen administratie- en btw-plicht. Eerst nog checken: de nevenwerkzaamheden-bepaling in het arbeidscontract, het vestigingsadres dat op de site moet, en de fiscale kant (urencriterium naast een baan van 32-40 uur) bij een boekhouder. |
