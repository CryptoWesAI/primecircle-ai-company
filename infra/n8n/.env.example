# Kopieer naar .env (gitignored) en vul een echte sleutel in.
# Genereer een sterke sleutel met:  openssl rand -hex 32
# LET OP: bewaar deze sleutel veilig. Raak je 'm kwijt, dan zijn opgeslagen
# credentials in n8n niet meer te ontsleutelen.
N8N_ENCRYPTION_KEY=vul-hier-een-openssl-rand-hex-32-sleutel-in
